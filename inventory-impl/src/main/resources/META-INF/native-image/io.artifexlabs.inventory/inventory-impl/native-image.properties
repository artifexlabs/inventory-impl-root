# Ulid holds a static SecureRandom; with Quarkus's build-time class init it
# would be baked into the native image heap with a cached seed. Initialize the
# class at run time instead so every image gets fresh randomness.
Args = --initialize-at-run-time=io.artifexlabs.inventory.impl.Ulid
